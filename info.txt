🐛 Gefundene Bugs

(aktuell keine offenen Bugs bekannt)
---
💡 Neue Ideen & Features

Zweisprachigkeit Deutsch/Englisch — Umschalter in den Spieler-Einstellungen
    Konkretisiert (06.07.2026): Kompletter Umfang (Spieler-Seiten + Admin-Bereich + docs/-
    Anleitungen + API-Meldungen + Push-Nachrichten). Zeitpunkt: erst angehen, wenn die
    anderen offenen Features erledigt sind — großes Projekt, mehrere Sessions.
    Ansatz (abgestimmt): Sprachdateien lang/de.php + lang/en.php (PHP-Arrays key => text)
    mit zentralem Helper t('key') in core/helpers.php; Sprache kommt aus neuer Spieler-
    Einstellung ww_lang (Allowlist in api/game.php, localStorage-Spiegel wie üblich,
    Default: de). Etappen-Plan:
    1. Infrastruktur: lang/-Dateien, t()-Helper, ww_lang-Setting + Umschalter im
       Einstellungs-Sheet (templates/nav.php) — klein, ca. 1-2h
    2. Spieler-Seiten: app/*, templates/nav.php, game-Blocks, Todesliste, Rollen-Galerie,
       FAQ, Statistik + zugehörige JS-Toasts/Confirms — größter Brocken, hunderte Strings
    3. DB-Inhalte: Rollen (name/description/rules), Dorf-Sprüche, Settings-Texte
       (login_title, deaths_empty_* etc.) brauchen je eine zweite Sprach-Spalte
       (z.B. description_en) + doppelte Eingabefelder in der Admin-UI; Anzeige wählt
       nach ww_lang mit Fallback auf Deutsch. Betreiber muss Inhalte selbst zweisprachig
       pflegen (auch künftig bei neuen Rollen/Sprüchen!)
    4. API-Meldungen + Push: jsonOk/err-Texte und WebPush-Titel/Bodies über t()-Keys;
       Achtung: Push geht an alle Spieler — Sprache pro Empfänger auflösen, nicht pro
       Auslöser. Logik-Texte wie "Vor Kummer gestorben" (recordDeath) mit übersetzen
    5. Admin-Bereich komplett + docs/spieler.php und docs/admin.php als Volltext-
       Übersetzung (reine Fleißarbeit)
    Risiken: hohes Regressionsrisiko weil praktisch jede Datei angefasst wird —
    etappenweise committen + nach jeder Etappe Live-Smoke-Test; {cooldown}-Platzhalter
    (roleText) muss in beiden Sprachvarianten der Rollentexte funktionieren.
Sheriff als bekannte Rolle — Permanent allen Spielern sichtbar; erhält nach jedem Kill zufällig einen garantierten Nicht-Killer angezeigt
Detektiv überarbeitet — Nach X Kills (= Anzahl der Killer) bekommt Detektiv einen zufälligen Nicht-Killer angezeigt; Fähigkeit ist passiv, kein Cooldown
Leichenfresser-Killer — Killt wie normal, aber Leichen können danach nicht vom Nekromant befragt werden und hinterlassen keine Orts-/Zeitspuren
Auftragskiller — Bekommt zufälliges Ziel zugewiesen; nach Kill neues Ziel (5 min Cooldown)
Schläfer/Verräter-Rolle — Killer der nach X Stunden die Seite wechselt; alle anderen Killer werden vorab gewarnt: "Einer von euch ist ein Verräter" (verzögerter Switch)
Detektiv: Leichen befragen — Bekommt von der Leiche einen Spieler genannt, der definitiv nicht der Killer war
Zombie-Spielmodus — Alternativer Modus: Alle starten als Söldner, nach 1–5h zufälliger Zombie-Start, Infizierte bekehren weitere Spieler
Günslinger: Weiter schießen nach Killer-Treffer — Wenn Günslinger einen Killer erwischt, darf er weiter schießen
Sprachnachrichten an den Spielleiter — Warum: einige Spieler schreiben nur ungern lange Texte
    und reden lieber direkt mit dem Spielleiter, Sprachnachricht senkt die Hemmschwelle.
    Im "Frage stellen"-Modal Auswahl Text/Sprachnachricht;
    Aufnahme per MediaRecorder-API (Mikrofon), Wiedergabe per <audio>-Player in der Admin-Nachrichtenverwaltung.
    Zu klären: Audioformat je nach Browser unterschiedlich (Chrome/Edge meist WebM/Opus, Safari eher MP4/AAC);
    passt nicht zur FAQ-Anonymisierung (Stimme verrät Identität stärker als Text), da Stimme die
    Identität stärker verrät als Text — Sprachnachrichten daher von der FAQ-Übernahme ausschließen,
    es sei denn die Transkriptions-Option (siehe unten) wird genutzt.
  Option: automatische Transkription per OpenAI-API (gpt-4o-mini-transcribe $0,003/Min. bzw.
    gpt-4o-transcribe/whisper-1 $0,006/Min.) — bei typischen kurzen Fragen (15-30s) nur Bruchteile
    eines Cents pro Nachricht, Kosten also kein Hindernis; würde einen OpenAI-API-Key in der Config
    brauchen (bisher hat das Projekt keine externen API-Abhängigkeiten).
  Erweiterung Admin-Bereich (Nachrichten-Verwaltung): Button "🎙️→📝 Audio zu Text" bei jeder
    Sprachnachricht — schickt die Aufnahme an die OpenAI-Transkriptions-API und trägt das Ergebnis
    direkt in das "FAQ-Text"-Feld ein (dasselbe Feld wie bei Text-Nachrichten, siehe messages.faq_question).
    Admin muss den transkribierten Text dann nur noch gegenlesen/anonymisieren statt selbst mitzuschreiben,
    bevor er die Antwort in die FAQ übernimmt.
  Erweiterung Admin-Einstellungen: An/Aus-Schalter für Sprachnachrichten (neue Settings-Zeile,
    z.B. voice_messages_enabled) — bei "Aus" verschwindet die Text/Sprachnachricht-Auswahl im
    "Frage stellen"-Modal komplett, Spieler sehen nur noch das normale Textfeld wie bisher.
  Eigener Akkordeon-Block "🎙️ Sprachnachrichten" in admin/settings.php (passend zur bestehenden
    Bereichs-Struktur): enthält den An/Aus-Schalter, das Eingabefeld für den OpenAI-API-Key
    (als eigene settings-Zeile, z.B. openai_api_key — Wert nur maskiert anzeigen wie ein
    Passwortfeld, analog zu SETUP_PASSWORD-Handling) und DB-Speicherung darüber wie bei den
    anderen Bereichen (eigenes Formular + eigener Speichern-Button, siehe bestehendes Muster).
---
✅ Gelöst (06.07.2026)

[Feature] Hellseherin: permanente Rollensicht — untersuchte Rollen dauerhaft sichtbar (→ v0.0.23)
      → Neues Rollen-Flag rollensicht (per Checkbox in der Rollen-Verwaltung für jede
        Rolle aktivierbar, Hellseherin = Standard). Ablauf im Spiel: untersuchten
        Spieler im Dorfbewohner-Block antippen (gleiche Bedienung wie beim Anklagen,
        auf Nutzerwunsch in v0.0.24 vom anfänglichen Auswahl-Popup darauf umgestellt),
        dann Button "🔮 Spieler untersuchen" drücken (Bestätigungs-Abfrage mit Namen;
        Ehrlichkeitsprinzip wie beim Mörder-Kill) — speichert die Erkenntnis und zeigt
        die Ziel-Rolle sofort als Toast; danach startet der normale Cooldown. Die Rolle bleibt ab dann
        dauerhaft in der Spielerliste sichtbar — serverseitig entschieden
        (get_players Regel e), nicht per DevTools austricksbar.
        Neue generalisierte Tabelle role_insights (game_id, viewer_player_id,
        target_player_id, source) — wie vom Betreiber vorgeschlagen als eigene
        "wer weiß was über wen"-Tabelle, normalisiert (eine Zeile pro Erkenntnis
        statt ID-Listen in einer Spalte), source-Spalte für künftige Mechaniken
        (Detektiv/Sheriff nutzen dieselbe Tabelle). Flag-Sichtbarkeiten (Mörder
        sehen sich) bleiben live berechnet — keine doppelte Wahrheit. reset_game
        räumt role_insights mit ab. Statistik: KPI "🔮 Untersuchungen" + im
        Spieler-Profil "Untersuchungen"/"Untersucht worden", get_stats-Versions-
        Probe um role_insights erweitert. Komplette Flag-Checkliste umgesetzt
        (Formular, collectFormData, role_create/update, Karten-Tag, dump_roles_temp,
        README); Rollen-Backup vorher gezogen (Scratchpad). Hinweis: Rollensicht
        braucht Cooldown > 0, sonst erscheint der Fähigkeit-Button nicht.

[Feature] Auto-Timeout zur Rollenkarte — nach X Minuten ohne Eingabe (→ v0.0.22)
      → Neue Spieler-Einstellung "Auto-Timeout zur Rollenkarte" (Optionen →
        🔒 Privatsphäre, Standard: aus; Auswahl 1/2/5/10 Minuten). Im Spielfenster
        läuft ein reiner Client-Timer (kein Serverkontakt): nach X Minuten ohne
        Eingabe (pointerdown/keydown/touchstart/scroll) öffnet sich automatisch die
        Rollenkarte als Sichtschutz — wie vom Nutzer gewünscht ausdrücklich an
        "keine Eingabe" gebunden, nicht an Seiten-Sichtbarkeit. Jede Eingabe startet
        den Timer neu; ist die Karte bereits offen, passiert nichts. Einstellungs-
        Änderung greift sofort (nav.php ruft window.restartRoleCardIdleTimer() auf).
        Neuer Settings-Key ww_rolecard_timeout in der Allowlist von api/game.php,
        localStorage-Spiegel + DB-Sync wie alle Spieler-Einstellungen. Doku in
        docs/spieler.php ergänzt. Kein DB-Schema-Update nötig (players.settings JSON).

[Feature] Cooldown-Platzhalter in Beschreibungen — {cooldown} in Rollentexten (→ v0.0.21)
      → In description/rules einer Rolle ersetzt die Anzeige {cooldown} automatisch
        durch den aktuellen Wert der cooldown-Spalte — Texte wie "alle {cooldown}
        Minuten" bleiben bei Cooldown-Änderungen ohne Handarbeit synchron. Zentrale
        Funktion roleText() in core/helpers.php, eingesetzt an allen Ausgabe-Stellen:
        game.php (Status-Block + Rollenkarten-Modal), roles_blocks.php (Galerie +
        Modal-JSON), app/roles.php (Modal-JSON), faq_blocks.php (Rollenregeln +
        Suchindex), role_card.php (Admin-Karte), docs/spieler.php (Anleitung;
        Query um cooldown ergänzt). Hinweis-Text im Rollen-Formular. Seed-Texte
        von Mörder + Hellseherin (fest "30 Minuten") in schema.sql/init.sql und
        auf der Live-DB auf den Platzhalter umgestellt — vorher per DB-Abgleich
        geprüft: nur diese beiden Rollen hatten feste Zeitangaben im Text, beide
        stimmten (noch) mit dem eingestellten Cooldown überein. Das Backup über
        dump_roles_temp.php zeigt weiterhin die Roh-Texte mit Platzhalter (korrekt,
        da es die Tabelle 1:1 sichert).

[Feature] Rollen-Presets — Gespeicherte Rollensets für verschiedene Spielerzahlen (→ v0.0.20)
      → Umgesetzt wie am 04.07. konkretisiert: Karte "🎛️ Rollen-Presets" oben in
        admin/roles.php — aktuellen Zustand aller Rollen (active, amount, fill) unter
        einem Namen speichern (z.B. "7 Spieler"), per Dropdown laden oder löschen.
        Speichern mit expliziter Auswahl: "➕ Neues Preset anlegen…" (Name-Feld) oder
        "Überschreiben: <Name>" (Name bleibt, Rückfrage im Client; Neuanlage mit schon
        vergebenem Namen wird serverseitig abgelehnt). Neue Tabellen role_presets +
        role_preset_items (normalisiert statt JSON-Snapshot, FK ON DELETE CASCADE:
        gelöschte Rollen fallen automatisch aus allen Presets). Neue API-Aktionen
        preset_save/preset_apply/preset_delete in api/admin.php. Regeln: max. 20
        Presets; beim Laden werden Rollen ohne Preset-Eintrag (nach dem Speichern neu
        angelegt) deaktiviert, damit das Preset immer die komplette Konfiguration
        beschreibt; leeres Preset (alle Rollen darin gelöscht) wird mit Fehlermeldung
        abgelehnt. Rollenliste rendert sich nach dem Laden ohne Seitenreload neu
        (Server liefert fertige role_card-HTML, wie bei role_update).
        Zusätzlich Spielstart mit Preset: im Admin-Dashboard (Lobby) neben "▶ Spiel
        starten" eine Preset-Auswahl + "▶ Mit Preset starten" (nur sichtbar wenn
        Presets existieren) — start_game akzeptiert optional preset_id und wendet das
        Set unmittelbar vor der Rollenverteilung an (zentrale Logik in
        applyRolePresetOrFail(), von preset_apply mitgenutzt). E2E auf dem Live-Server
        getestet: Anlegen/Überschreiben/Duplikat-Ablehnung, Start mit 21 Testspielern
        (11 Sonderrollen + 10 Bürger, Preset-Name in der Meldung), reset + Aufräumen.

[Review] Re-Review der v0.0.18-Fixes — 7 Findings behoben (→ v0.0.19)
      → Sequenzieller 8-Winkel-Durchlauf mit Einzel-Verifikation (13 Kandidaten,
        6 widerlegt). Behoben: (1) Versammlungs-Sperre war per "Einberufen + vor
        Beginn wieder Beenden" aushebelbar — hangedThisAssembly() zählt jetzt nur
        tatsächlich gestartete Versammlungen (scheduled_at <= jetzt) als Fenster.
        (2) reset_game räumt assembly_requests mit ab (sonst: Alt-Versammlung lebt
        im neuen Spiel weiter + dauerhafter Hinrichtungs-Lockout). (3) Icon-Cache-
        Header gelten jetzt auch für 304 — Browser ersetzen bei Revalidierung die
        gespeicherten Header, mit no-cache wäre das Caching nach 7 Tagen dauerhaft
        erloschen (live per curl belegt). (4) Cooldown-Formel zentral in
        cooldownRemainingSecs() mit Clamp [0, total] gegen DB-Uhr-Rücksprünge.
        Cleanups: gamePlayer() liefert cooldown_elapsed mit (Extra-Query weg),
        hangedThisAssembly() in einer Query, Funken-Bootstrap ruft openRoleCard().

[Review] Code-Review v0.0.17 — alle 13 bestätigten Findings behoben (→ v0.0.18)
      → Schwerste Punkte: (1) Cooldown-Zeitzonen-Regression — Server sendet jetzt nur noch
        verbleibende Sekunden (remaining_secs, TIMESTAMPDIFF auf der DB-Uhr), Client zählt
        lokal herunter; keine Zeitstempel-Vergleiche PHP/DB/Browser mehr. (2) „Max. 1
        Hinrichtung pro Versammlung" war an round gekoppelt und sperrte den ganzen Tag —
        neuer Helper hangedThisAssembly() prüft seit Beginn der letzten Versammlung
        (auch vom Admin-Dashboard genutzt, Duplikat-SQL weg). (3) Phantom-Hinrichtung bei
        totem Ziel abgefangen. (4) execute_vote: player_id muss dem Top-Angeklagten
        entsprechen, deterministischer Tiebreak (kleinere ID) identisch in API+Dashboard.
        (5) GET_LOCK gegen parallele Doppel-Hinrichtungen. (6) Auto-Rollenkarten-Funken
        prüfen Overlay-Klasse statt localStorage + Interval-Leak in openRoleCard() behoben.
        (7) Icon-Cache-Header nur noch bei Status 200 (404/403 bleiben no-cache), immutable
        entfernt, fehlende ?v=-Cache-Buster in Rollen-Formular + Spieler-Anleitung ergänzt.
        Dazu Cleanup: playerSettings()-Request-Cache, jsonOk() statt hand-echotem JSON im
        Cooldown-Endpunkt, Sperr-Grund im Voting-Block nur noch einmal formuliert.
        Details siehe CHANGELOG v0.0.18. Live deployt + verifiziert (php -l, Header-Test
        200/404, Helper-Test gegen Live-DB, Statuscode-Smoke aller betroffenen Seiten).
---
✅ Gelöst (05.07.2026)

[Feature] Startseite = Rollenkarte — App öffnet auf Wunsch direkt die eigene Rollenkarte
      → Neue Spieler-Einstellung "Rollenkarte beim Öffnen zeigen" (Standard: aus,
        Optionen → 🔒 Privatsphäre) — Grund: beim erneuten Einloggen sollen andere
        Mitspieler, die zusehen könnten, nicht kurz das Spielfenster mit Infos sehen,
        die nur der Karteninhaber sehen darf. Bei aktivierter Einstellung rendert
        app/game.php das #role-card-overlay serverseitig direkt mit der Klasse "open"
        (statt es erst per JS nach dem Laden zu öffnen) — dadurch erscheint die Karte
        sofort beim ersten Seitenaufbau, ohne dass das Spielfenster auch nur kurz
        aufblitzt. Neuer Settings-Key ww_auto_rolecard (Allowlist in api/game.php,
        localStorage-Spiegelung wie alle anderen Spieler-Einstellungen). Dokumentiert
        in docs/spieler.php.
---
✅ Gelöst (04.07.2026)

[Feature] Voting-System — Mindestens 2 Spieler müssen für eine Hinrichtung stimmen
      → Feste Spielregel MIN_VOTES_TO_HANG (= 2, core/bootstrap.php, kein Admin-UI-Feld,
        da keine Konfigurierbarkeit gewünscht). Der „Hängen"-Button im Admin-Panel ist
        deaktiviert, solange der Angeklagte mit den meisten Stimmen nicht mindestens diese
        Anzahl erreicht hat (Hinweistext zeigt die fehlende Stimmenzahl). Zusätzlich
        serverseitig abgesichert: api/admin.php → execute_vote prüft die Stimmenzahl für
        das Ziel und lehnt die Hinrichtung mit Fehlermeldung ab, falls sie nicht erreicht
        wird — Umgehung über die API direkt (z.B. per curl) ist damit ausgeschlossen.
        Zusätzlich: max. 1 Hinrichtung pro Bürgerversammlung — wurde in der aktuellen
        Runde bereits jemand gehenkt, bleibt der „Hängen"-Button für alle weiteren
        Angeklagten dieser Versammlung gesperrt (geprüft über game_id+round in der
        deaths-Tabelle), unabhängig davon, wie viele Angeklagte/Stimmen es gibt.
[Bug] Cooldown startet bei falschem Wert — Nach Klick beginnt Timer nicht bei 0, sondern bei 20 Sekunden
      → Ursache: api/game.php (start_cooldown) schrieb den Startzeitpunkt per MySQL NOW() in
        die DB, gab dem Client aber einen separat mit PHP date('c') berechneten Zeitstempel
        zurück — zwei unterschiedliche Uhren (Web-Server-Prozess vs. DB-Server), die schon bei
        geringer Drift (z.B. Docker/WSL2-Container-Uhr) einen sofortigen Rückstand zeigten.
        Fix: Nach dem UPDATE wird der tatsächlich gespeicherte cooldown_started_at-Wert per
        SELECT zurückgelesen und als ISO-Zeitstempel (GAME_TIMEZONE) an den Client geschickt —
        Client rechnet jetzt exakt mit dem Wert, der auch in der DB steht.

✅ Gelöst (03.07.2026)

[Bug] Seite refresht sich beim Befragen — Popup verschwindet durch automatisches Neuladen mitten im Vorgang
      → Totenliste nutzt jetzt Live-Updates statt Komplett-Reload; Popup bleibt offen
[Bug] Suche springt nicht zum Treffer — öffnete beide Bereiche (FAQ + Rollenregeln) gleichzeitig
      → Suche wechselt automatisch zum Tab mit Treffern, scrollt zum ersten Treffer, schließt beim Leeren wieder
[Bug] Cooldown-Wert: Integer-Overflow — Zu hohe Zahl ergab "Netzwerkfehler" statt klarer Fehlermeldung
      → Begrenzung auf 0–10080 Minuten (7 Tage) mit klarer Fehlermeldung, auch im Formularfeld
[Bug] Rollen der Toten im Admin-Menü sichtbar — sollte verborgen sein
      → Admin-Spielerliste zeigt bei Toten keine Rolle mehr (kein Spoiler für mitspielenden Admin)
[Bug] Testspieler anlegen kumuliert nicht — 5 anlegen, nochmal 5 → kein Neuzugang
      → Es werden jetzt die nächsten freien Nummern belegt; 5 + 5 = 10
[Bug] Große Rollenkarte: Spielerseite scheint hinter der Karte durch
      → Overlay ist jetzt voll deckend bis zum Bildschirmrand
[Feature] Live-Update ohne Seiten-Reload — AJAX/JavaScript für Echtzeit-Updates
      → Komplett umgesetzt: alle Seiten aktualisieren sich ohne Reload, Intervall in den
        Spieler-Einstellungen wählbar, Countdown in der Kopfleiste, Übertragung nur bei Änderungen
[Bug] Anklagen ohne Versammlung möglich — Spieler konnte angeklagt werden, obwohl keine Versammlung aktiv ist
      → Anklagen sind nur noch während einer laufenden Versammlung möglich (Server-Prüfung);
        die Anklage-Karte erscheint erst mit Versammlungsbeginn und verschwindet beim Ende
[Feature] Versammlung startet erst ab 2 Spielern — einer allein reicht nicht
      → Erster Spieler stellt den Antrag (für alle sichtbar + Push), ein zweiter muss
        unterstützen, erst dann steht der Termin (nächste volle Stunde); beenden können
        nur die beiden Einberufer (oder die Spielleitung); Antragsteller kann zurückziehen
[Feature] Dodo-Animation bei Hinrichtung — Große Karte/Effekt wenn Dodo gehängt wird
      → Vollbild-Auftritt mit Federregen bei allen Spielern, sobald der Dodo gehängt wird;
        über den Effekte-Schalter "Phasen-Überblendung" deaktivierbar
[Feature] Effekte verbessert — Partikel mit Tiefenwirkung, dichterer Nachtnebel (5 Schichten),
        Sterne/Sonnenstrahlen in der Phasen-Überblendung, roter Puls bei Todesfall,
        Glocke bei Versammlungsbeginn; Partikel-Loop pausiert in versteckten Tabs (Akku)
[Bug] Dorfbewohner-Block: Spaltenzahl je nach Theme unterschiedlich (z.B. Gothic)
      → Feste Spaltenzahl (4 / mobil 3 / klein 2) statt auto-fill — in allen Themes identisch
[Bug] Admin-Einstellungen wurden nicht gespeichert + kein VAPID-Key erstellbar
      → settings-INSERT scheiterte im MariaDB-Strict-Modus (Pflichtfeld label ohne Default);
        Speicherroutine gefixt, Tabellen-Defaults ergänzt, VAPID-Schlüssel ist bereits erzeugt
[Bug] Admin-Einstellungen: Formular übertrug bei JS-Fehler alle Werte per GET in der URL
      → method="POST" als Absicherung gesetzt
[Feature] Rollen-Flag "Gegenseitig sichtbar mit Killern" (z.B. für den Dodo)
      → Neues Häkchen in der Rollen-Verwaltung: Rolle und Mörder sehen sich gegenseitig
        in der Spielerliste; Hinweis auf der Statuskarte; Tag in Rollen-Galerie/FAQ/Admin
[Feature] Wichtige Push-Benachrichtigungen ohne Cooldown
      → Phasenwechsel, Hinrichtung und Todesfälle umgehen jetzt den Push-Cooldown
        (wie Spielstart/-ende und Versammlungen); der Cooldown greift nur noch
        für unwichtige Benachrichtigungen
[Bug] Spiel-Reset ließ den alten Gewinner (winner) stehen
      → reset_game setzt winner jetzt auf NULL zurück
[Bug] Admin-Einstellungen: Debug/Beta-Schalter wurden nicht gespeichert
      → Auto-Save stürzte seit Einführung still ab (saveSettings(null) rief
        e.preventDefault() auf null auf); gefixt — Schalter speichern wieder
        automatisch; Fehlermeldung zeigt jetzt die echte Server-Antwort statt
        nur "Netzwerkfehler"
[Feature] Test-Button für Push-Benachrichtigungen in den Admin-Einstellungen
      → Push-Sektion hat jetzt "Test-Benachrichtigung": 🔔 An mich senden
        (nur eigene Geräte) und 📣 An alle Geräte (mit Rückfrage); sendet
        sofort ohne Cooldown — vorher gab es den Test nur versteckt in der
        System-Diagnose
[Feature] Admin-Einstellungen komplett neu gebaut: pro Bereich eigenes Formular
      → Jeder Bereich (Allgemein/Spiel/Push/Design/System/Texte) speichert jetzt
        unabhängig mit eigenem Speichern-Button + eigener Fehleranzeige — ein
        ungültiger Wert in einem Bereich blockiert nicht mehr die anderen;
        Sprungmarken-Leiste oben auf der Seite springt direkt zum Bereich
[Bug] Admin-Einstellungen: ECHTE Ursache für "Debug/Beta speichert nicht" +
      "Gerät löschen geht nicht" + "Test-Push reagiert nicht" gefunden
      → admin/settings.php deklarierte "const SETTINGS_API", genau wie
        templates/nav.php (dort für die Spieler-Einstellungen, auf JEDER
        Seite geladen). Doppelte "const"-Deklaration im selben globalen
        Scope = SyntaxError im Browser ("Identifier 'SETTINGS_API' has
        already been declared") — das killt das KOMPLETTE zweite Script,
        nichts darin wird ausgeführt (kein Hoisting bei Syntax-Fehlern,
        anders als bei Laufzeitfehlern). Dadurch liefen saveSection,
        deleteSub, clearAllSubs, testPush etc. seit jeher ins Leere, obwohl
        der Server bei jedem direkten Test einwandfrei funktionierte — der
        Klick hat den Server nie erreicht. Konstante in admin/settings.php
        zu ADMIN_SETTINGS_API umbenannt, alle Vorkommen angepasst.
      → Lehre: Server-seitige Tests per curl/HTTP prüfen nie das Browser-
        JavaScript — ein Blick in die DevTools-Console war hier der
        einzige Weg, den Fehler zu finden.
[Feature] Admin-Einstellungen: Bereiche als Akkordeon statt durchgehender Liste
      → Alle 7 Bereiche starten zugeklappt und öffnen sich einzeln per Klick
        auf den Titel (Chevron dreht sich beim Öffnen) — spart Platz, vor
        allem am Handy; immer nur EIN Bereich gleichzeitig offen (Öffnen
        eines Bereichs schließt automatisch alle anderen); Sprungmarken-
        Leiste oben wieder entfernt (durch Exklusiv-Akkordeon überflüssig);
        jeder Bereich hat unten einen "↑ Nach oben"-Link zurück zum Seitenkopf
[Feature] Root-Verzeichnis aufgeräumt — Unterseiten in app/ verschoben
      → Nur noch index.php liegt im Root; game/deaths/roles/stats/faq/logout/
        register/datenschutz/impressum/nutzungsbedingungen/dump_roles_temp
        liegen jetzt unter app/; sw.js liegt unter assets/js/sw.js (Scope
        explizit auf "/" gesetzt, sonst wäre Push-Registrierung site-weit
        kaputt gegangen — Service-Worker-Allowed-Header in .htaccess ergänzt);
        neues Verzeichnis app/ ist wie config/core/templates/db gegen
        Verzeichnislisting geschützt (403 bei direktem Aufruf von /app/);
        alte Root-URLs (z. B. /game.php) leiten per 301 automatisch auf
        /app/… um — bestehende Bookmarks/PWA-Icons/Links bleiben nutzbar
[Bug] Nach dem Beantworten einer Spielerfrage fehlte der "FAQ freigeben"-Button
      bis zum nächsten Seiten-Reload
      → sendReply() patchte bisher nur einzelne DOM-Stellen von Hand und vergaß
        dabei den Button, der serverseitig nur bei bereits beantworteten Fragen
        gerendert wird; behoben, indem reply/toggle_publish/set_faq_question
        jetzt die komplette Zeile fertig gerendert zurückgeben (render_message_row)
        und der Client sie 1:1 ersetzt — dieselbe Quelle wie beim Seitenaufbau
[Feature] FAQ-Übernahme jetzt anonymisierbar
      → Neuer Button "✏️ FAQ-Text" bei jeder beantworteten Frage: Admin kann vor
        der Veröffentlichung einen eigenen, anonymisierten Text hinterlegen
        (neue Spalte messages.faq_question) — die Original-Nachricht des Spielers
        bleibt dabei unverändert erhalten, nur die öffentliche FAQ zeigt den
        bearbeiteten Text (COALESCE(faq_question, message)); Hinweis beim
        Fragenformular (auffällige Warnbox) und in der Spieler-Anleitung ergänzt:
        keine Namen/persönlichen Angaben in Fragen schreiben, da sie anonym in
        die FAQ übernommen werden können
[Feature] Admin: Tote wiederbeleben (Debug-Menü)
      → Neue Seite admin/debug.php, nur im Debug-Modus nutzbar: 🔮 Tote wiederbeleben
        (setzt is_alive=1 zurück und löscht den Todeslisten-Eintrag, ohne neue Runde
        zu starten) sowie die bisherige "Eigene Rolle wählen"-Funktion, aus dem
        Dashboard hierher verschoben, damit die Spielleitung übersichtlich bleibt.
        Neue API-Aktion revive_player (api/admin.php), hinter APP_DEBUG-Check wie
        set_own_role. Testspieler-Verwaltung ebenfalls hinter APP_DEBUG gesperrt
        (Seite + alle drei AJAX-Aktionen) und nur noch im Debug-Modus in der
        Kachel-Liste sichtbar. Kachel-Reihenfolge im Verwaltungsbereich neu sortiert
        nach Gebrauchshäufigkeit im laufenden Spiel: Nachrichten, Rollen, Spieler,
        Dorf-Sprüche, Einstellungen, Systemcheck, Setup, Debug-Menü, Testspieler.
[Bug] Paar: zweiter Spieler stirbt nicht automatisch mit
      → Neues Rollen-Flag `linked_death` (z.B. für "Das Paar"): Stirbt ein Spieler
        dieser Rolle, sterben automatisch alle anderen lebenden Spieler derselben
        Rolle im selben Spiel mit. Der erste Tod läuft komplett normal (eigener
        Ort, normale Regeln). Der automatisch mitsterbende Partner bekommt als Ort
        fest "Vor Kummer gestorben" — bleibt aber wie jeder andere Tod erst mal
        verborgen (rolle_aufgedeckt=0), sichtbar wird es nur über die bestehende
        Nekromant-/Selbstauskunft-Funktion, keine Sonderbehandlung.
        Umsetzung folgt der Rollen-Flag-Checkliste aus CLAUDE.md: db/schema.sql +
        db/init.sql (Spalte + Migration für bestehende Installs, "Das Paar" auf 1
        gesetzt), role_form_fields.php (Checkbox), admin/roles.php JS, api/admin.php
        (role_create/role_update), role_card.php (Tag), dump_roles_temp.php
        (Spaltenliste), Spiellogik in recordDeath() (core/helpers.php), Doku in
        README.md und docs/spieler.php ergänzt. Live getestet: zwei Testspieler auf
        "Das Paar" gesetzt, einen getötet — Partner starb automatisch mit korrektem
        Ort, beide zunächst unaufgedeckt wie jeder normale Tod.
[Bug] Rollen-Liste (Admin): Play/Pause-Schalter für Aktiv/Inaktiv war vertauscht
      → War fälschlich in der Bug-Liste als "Versammlungs-Button" notiert — tatsächlich
        betroffen war der Aktiv/Inaktiv-Schalter jeder Rollenkarte in admin/roles.php:
        aktive Rolle zeigte ⏸ (Pause), inaktive Rolle zeigte ▶ (Play) — invertiert.
        Statt nur die Icon-Zuordnung zu tauschen, komplett auf den bestehenden
        Toggle-Switch (wie in den Admin-Einstellungen) umgebaut: eindeutiger
        An/Aus-Schalter ohne Symbol-Mehrdeutigkeit, macht diese Bug-Klasse hier
        strukturell unmöglich. Live getestet: deaktivieren/aktivieren über die
        echte API, Checkbox- und Karten-Zustand stimmen in beide Richtungen.