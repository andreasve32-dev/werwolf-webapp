# ── Zugangsdaten (NIEMALS ins Repo) ─────────────────────────
config/config.php

# ── Privater Update-Signaturschlüssel (NUR lokal, nie ins Repo!) ─
config/update_privkey.secret
updates/*.wwupd
updates/backups/

# ── Backup-ZIPs ──────────────────────────────────────────────
public/

# ── Audiodateien (zu groß für Git) ───────────────────────────
audio/*.mp3
audio/*.ogg
audio/*.wav

# ── Nutzer-Uploads (Sprachnachrichten etc.) ──────────────────
uploads/

# ── Claude-Sitzungsdaten ─────────────────────────────────────
.claude/

# ── Lokale Notizen (Bugs/Ideen), nie ins Repo ────────────────
info.txt

# ── Anwendungs-Logs (nur die Schutz-.htaccess ins Repo) ──────
logs/*
!logs/.htaccess

# ── PDFs / lokale Dokumente ──────────────────────────────────
*.pdf

# ── Betriebssystem ───────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
